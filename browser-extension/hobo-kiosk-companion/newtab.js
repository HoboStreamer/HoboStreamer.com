// Every new tab lands on the kiosk with the address bar open (#input).
// The content script then wires history autocomplete into that page.
location.replace("https://hobostreamer.com/kiosk#input");
